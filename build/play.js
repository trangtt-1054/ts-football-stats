"use strict";
/* const addOne = (a: number, b: number): number => {
  return a + b;
};

class HoldAnyThing<TypeOfData> {
  data: TypeOfData;
}

const holdNumber = new HoldAnyThing<number>();
holdNumber.data = 123;

const holdString = new HoldAnyThing<string>();
holdString.data = 'ahihi'; */
//generics: a type as an argument to the class, just like argument to a function, cái ngoặc khi instantiate luôn có, chỉ khác là thêm <> đựng type
/* class HoldNumber {
  data: number;
}

class HoldString {
  data: string;
}

const HoldNumber = new HoldNumber();
const HoldString = new HoldString(); */
